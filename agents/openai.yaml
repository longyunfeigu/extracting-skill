interface:
  display_name: "Skill X-Ray"
  short_description: "Trace a skill's recommended path, checkpoints, and behavior changes."
  default_prompt: "Study this skill package and produce a Skill X-Ray with trace type, auto decisions, checkpoint map, Intervention Map, evidence table, friction score, and upgrade options."
